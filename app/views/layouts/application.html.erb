<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Year End" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div id="title"><%= link_to('Year End', root_path) %>
    <% unless user_signed_in? %>
    <div class="title-right"><%= link_to('Sign In', new_user_session_path)  %></div>
    <div class="title-left"><%= link_to('Sign Up', new_user_registration_path) %></div>
    <% end %>
    <% if user_signed_in? %>
    <div class="title-left" id="header-pic"><%= image_tag current_user.avatar %></div>
    <div class="title-left"><%= link_to('My Profile', user_path(current_user.id)) %></div>
    <div class="title-right"><%= link_to('Sign Out', destroy_user_session_path, method: :delete) %></div>
    <% end %>
    </div>
    <p class="flash"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
